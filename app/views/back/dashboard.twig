<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouDemy - Tableau de bord administrateur</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css" rel="stylesheet">
</head>
<body class="bg-gray-50">

    <div class="min-h-screen flex">
        <!-- Sidebar -->
    {% include('partials/admin/sidebar.twig') %}
        <!-- Main Content -->
        <main class="flex-1 p-6">
            <div class="flex items-center justify-between mb-6">
                <h1 class="text-2xl font-bold text-gray-900">Bienvenue, {{ user.name }}</h1>
                <a href="/logout"
                   class="rounded-md bg-emerald-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-emerald-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-emerald-600 flex items-center">
                    <span class="ml-1">Déconnexion</span>
                    <i class="ri-logout-circle-r-line text-xs ml-1"></i>
                </a>
            </div>

            <!-- Dashboard Stats -->
            <div class="grid grid-cols-1 md:grid-cols-2  gap-6">
                <div class="bg-white shadow-md rounded-lg p-6 text-center">
                    <h3 class="text-xl font-bold text-emerald-600">11</h3>
                    <p class="text-sm text-gray-600">Utilisateurs totaux</p>
                </div>
                <div class="bg-white shadow-md rounded-lg p-6 text-center">
                    <h3 class="text-xl font-bold text-emerald-600">22</h3>
                    <p class="text-sm text-gray-600">Articles publiés</p>
                </div>
            </div>
        </main>
    </div>
</body>
</html>